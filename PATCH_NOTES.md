# RHODES OBS COMMANDER3373 更新内容（2026-09-08・サルカズOCR改善）

対象: 前回配布 `20260908-063809-ae1a1bf-slim.zip` 以後の更新

## 秘宝のスタック数認識

- スタック表示の目印と数字を分けて読み取り、背景や装飾を数字として拾うケースを減らしました。
- 数字が読み取れなかった場合だけ、明暗・白黒化・余白を変えて再確認します。確定済みのカードは追加処理を打ち切ります。
- Friston.Pの8・11・12・14、丸い石の祭壇と論断（術師／補助／先鋒）の各1など、従来読み取れなかった表示への補正を追加しました。
- 同じ画像を複数の方法で読み直しても、独立した複数票として数えないようにしました。同一画像内で個数が競合する場合は確定しません。
- 文字混入、複数の数字、範囲外の数値、低い確信度の結果は採用しません。個数を確定できない場合は手入力済みの値を保持します。

## 名称認識と長い思案一覧

- オペレーター・秘宝・思案の名称補正を共通化しました。正式名称を優先し、登録済みの誤読だけを補正します。説明文の一部だけを名称として拾う補正は避けます。
- メイ、ムリナール、トラゴーディア、12Fなどの誤読と、1文字の正式オペレーター名の読み取りを改善しました。
- 啓蒙、開墾、功名の石柱、土を被った食器などの思案名と、『帰還』の誤読を補正しました。
- 背景や説明文と重なった秘宝名、読み取れなかった思案名は、対象範囲を拡大・補正して再確認します。再確認結果が正式名称と一致し、十分な確信度がある場合だけ採用します。
- 思案一覧のスクロール上限を拡張し、長い一覧が従来の上限で途中終了しにくくしました。画面末尾の判定とキャンセルは継続して使用します。
- 名称補正データを検証用・配布用の両方に含めました。

## 確認した範囲

- スタック数の検証用画像157ケースを確認しました。個数表示のある144ケースは全件一致し、個数表示のない13ケースでは個数を誤補完しませんでした。
- 上記は検証した画像の範囲での結果です。すべての解像度、画面状態、表示条件での完全な認識を保証するものではありません。

## 確認結果

- Node.jsテスト: 535件成功
- C#サービステスト: 337件成功
- MAAリソース・契約チェック: 成功
- Suki/Avaloniaビルド: 成功（警告・エラーなし）
- 上記のスタック画像検証と自動テストを実施しました。配布EXEの実画面操作とOBS本体での確認は今回行っていません。

## 更新方法と利用時の注意

- 配布ZIPを新しいフォルダーへすべて展開し、`RhodesSuki.exe` を起動してください。EXEだけを旧フォルダーへ差し替えず、認識データを含む一式で更新してください。
- 既存設定を引き継ぐ場合は、3373を終了してから旧フォルダーの `user-data` を新しいフォルダーへコピーしてください。元のフォルダーは確認が終わるまで保管してください。
- 今回の配布も軽量版です。配信サーバー・簡易公開に必要なランタイムは必要時に取得します。
- 読み取り結果は画面で確認し、必要に応じて手動で訂正してください。所持一覧から消えた秘宝の自動削除と、ゲーム内だけで始めた新規ランの画像による確実な判定は引き続き未対応です。
- 歳の銭OCR（有効銭・保有銭）は引き続き停止中です。銭は手動入力してください。
