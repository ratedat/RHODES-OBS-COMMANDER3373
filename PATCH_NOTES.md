# RHODES OBS COMMANDER3373 更新内容（2026-09-08）

対象: 前回配布 `20260901-010504-637311c-slim.zip` 以後の更新

## 大会遠隔入力の復旧と担当交代

- 入力担当画面で下書きへ追加した内容をブラウザー内に保存し、ページを再読み込みしても復元するようにしました。
- 送信前に操作IDを保存し、通信切断や応答待ちのタイムアウト後も同じIDで結果確認・再送します。中継側で同じ操作を照合するため、受理済みの変更を重複して反映しません。
- 送信済み、反映待ち、再送可能、前担当の反映待ち、端末保存エラーを入力画面で区別して表示します。
- 前の変更が反映待ちの間は、新しい編集と送信を止めます。別端末へ担当を交代した場合も、前担当の結果が確定するまで次の送信を受け付けません。
- 拒否された変更は元の端末で下書きへ戻し、内容を直して再送できるようにしました。
- 配信PCからの結果通知や状態同期が一時的に失敗した場合は再試行し、保存済みの操作を再適用しないようにしました。遅れて届いた古い状態で入力画面や保存状態が巻き戻ることも防ぎます。
- 大会入力を開始した後の保存済みラン状態を、3373の再起動後にも引き継ぐようにしました。「ランをクリア」すると引き継ぎも解除されます。
- 1セッションの操作上限は500回です。一括送信は1回として数え、上限後も再送照合用の履歴は保持します。上限に達した場合は、保存済み状態を確認して新しいセッションを開始してください。

## 認識と状態保存の安全性

- 画面移動後は安定した連続フレームを確認してから認識し、撮影失敗や安定待ちの期限切れを成功として扱わないようにしました。
- 試行上限に達した未解決カードを解決済みにせず、キャンペーン変更、ランの手動クリア、再接続、認識リソースの再読み込み時には終端キャッシュを消去します。
- 人数一致や画像の類似だけを理由に別の取得結果を再利用しません。昇進、アーミヤの職分、秘宝交換、スタック数の変化を省略しない安全側の動作にしました。
- スタックOCRは、装飾や余分な文字を含む曖昧な結果と、同じ画像内で値が競合する結果を拒否します。読み取れない個数を1個と推定しません。
- 認識操作の結果を `完了`、`一部完了`、`失敗`、`キャンセル` に分け、画面復帰、候補処理、状態保存を含む一連の記録を残すようにしました。過去の記録を新しい処理で上書きしません。
- 保存状態を読み込めない場合は初期状態で上書きせず、エラーを表示して元のファイルを保持します。同じ状態ファイルを使う二重起動でも、2つ目の起動が現在のランを初期化しないようにしました。

## 起動・配布まわり

- ソースフォルダーからの起動は、現在のソースのビルドに成功した場合だけアプリを開くようにしました。
- 起動時に既存のローカルサーバーを自動終了しません。ポートが使用中の場合は、起動済みのアプリを使うか別ポートを選んでください。
- ビルドに使う依存関係と認識用ファイルの確認を固定し、配布ZIPの作成前に不足や意図しない差分を検出するチェックを追加しました。
- 配布ZIP内の日本語ファイル名をUTF-8で保存し、解凍ソフトによる文字化けを防ぐようにしました。

## 利用時の注意

- 大会入力を開始した後の保存済みラン状態は再起動後も残りますが、接続は自動再開しません。状態を確認して大会入力を開始し直し、新しい入力URLとコードを担当者へ渡してください。
- 外部中継サーバーを使う場合は、3373本体と中継サーバーの両方を更新してください。
- 未送信の下書きは元のブラウザーに残ります。別端末への自動移送、ブラウザーの保存データを削除した後の復元、下書きへ追加する前の入力欄の値の復元には対応していません。
- 入力画面の履歴表示は直近100回です。再送照合に必要な履歴は、500回のセッション上限まで中継側で保持します。
- 所持一覧から消えた秘宝の自動削除と、ゲーム内だけで始めた新規ランの画像による確実な判定は未対応です。必要な場合は手動で訂正または「ランをクリア」してください。
- OCRの実画像に対する正解率と、取得操作全体の速度改善率は今回確定していません。

## 確認結果

- Node.jsテスト: 524件成功
- C#サービステスト: 325件成功
- MAAリソース・契約チェック: 成功
- Suki/Avaloniaビルド: 成功
- ローカル環境で、入力担当画面から中継、配信PC API、1920×1080のOBS Browser Source用画面までの状態反映を確認済みです。下書き再読込、応答喪失後の重複防止、担当交代も確認しています。再起動時の保存状態引き継ぎは自動テストで確認しました。
- 実際のインターネット回線、Suki/Avaloniaの実画面操作、OBS本体での確認は未実施です。
